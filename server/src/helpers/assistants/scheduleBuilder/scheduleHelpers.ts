// import { getScheduleById } from "../../../db/models/schedule/scheduleCollection";

// export const getScheduleById = async (userId: string, scheduleId: string) => {
//   const schedule = await getScheduleById(userId, scheduleId);
//   //   TODO: Maker sure the return type is correct

//   return schedule;
// };
